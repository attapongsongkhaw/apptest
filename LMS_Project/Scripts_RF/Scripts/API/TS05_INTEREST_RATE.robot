*** Settings ***
Force Tags        api
Library           DatabaseLibrary

*** Test Cases ***
New Interrest rate
    Log    ${TEST NAME}

Edit Interrest rate
    Log    ${TEST NAME}

Delete Interrest rate
    Log    ${TEST NAME}

Disable/Enable Interrest rate
    Log    ${TEST NAME}

Authorize Interrest rate
    Log    ${TEST NAME}
